# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Heroes' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Heroes
  pod 'ObjectMapper'
  pod 'Alamofire'
  pod 'CryptoSwift'
  pod 'SwiftyJSON'
  pod 'Kingfisher', '~> 4.0'
  pod 'Hero'
  pod 'lottie-ios'

  target 'HeroesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HeroesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
